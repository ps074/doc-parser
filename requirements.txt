# Document Parsing Libraries

# Core pipeline (page_classifier + pipeline.py)
PyMuPDF>=1.25.0
pypdfium2>=4.0.0
anthropic[vertex]>=0.40.0
openai>=1.0.0
google-genai>=1.0.0

# Docling + VLM Parser
docling>=2.0.0
docling-ibm-models>=2.0.0

# Google Gemini Parser
google-cloud-documentai>=3.10.0
google-auth>=2.0.0

# Unstructured.io Parser (requires tesseract: brew install tesseract)
unstructured[all-docs]>=0.10.0

# pdfplumber Parser
pdfplumber>=0.10.0
Pillow>=10.0.0

# Chunking (parsers/chunker.py)
chonkie>=1.0.0

# Chunking evaluation (experiments/chunking/)
tiktoken>=0.7.0
numpy>=1.24.0
rank-bm25>=0.2.2
pymupdf4llm>=0.0.10

# TurboPuffer retrieval (optional, experiments/chunking/store.py)
# turbopuffer>=0.3.0

# Common utilities
python-dotenv>=1.0.0
requests>=2.31.0

# System dependencies:
# - tesseract OCR: brew install tesseract (for unstructured hi_res strategy)
